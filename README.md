# Система управления экскурсионными турами

## Описание проекта

Консольное приложение для управления базой данных экскурсионных туров 
на Python с использованием PostgreSQL. Проект предоставляет полный 
функционал CRUD операций для работы с городами и туристическими 
маршрутами.

## Структура базы данных

### Таблица City (Города)
- **id** - суррогатный ключ (SERIAL, PRIMARY KEY)
- **name** - название города (VARCHAR(100), UNIQUE, NOT NULL)

### Таблица Route (Маршруты)
- **id** - суррогатный ключ (SERIAL, PRIMARY KEY)
- **name** - название маршрута (VARCHAR(255), NOT NULL)
- **departure_city_id** - город отправления (INT, FK -> City.id)
- **description** - описание маршрута (TEXT, необязательно)
- **base_price** - базовая цена в рублях (NUMERIC(10,2), NOT NULL, >= 0)

### Связи между таблицами
- Route.departure_city_id → City.id (внешний ключ)
- При попытке удаления города проверяется наличие связанных маршрутов

## Возможности системы

### Работа с городами
- ✅ Просмотр списка всех городов с постраничной навигацией
- ✅ Добавление нового города
- ✅ Редактирование существующего города
- ✅ Удаление города (с проверкой связанных маршрутов)
- ✅ Проверка уникальности названий городов

### Работа с маршрутами
- ✅ Просмотр маршрутов для выбранного города
- ✅ Добавление нового маршрута
- ✅ Редактирование существующего маршрута
- ✅ Удаление маршрута
- ✅ Постраничная навигация по маршрутам

### Особенности интерфейса
- ✅ Пользователь работает только с порядковыми номерами и данными
- ✅ Суррогатные ключи (ID) не отображаются в интерфейсе
- ✅ Постраничный просмотр (10 записей на страницу)
- ✅ Навигация между страницами: [ ] для пред./след. страницы

## Реализованные требования безопасности

### Защита от SQL-injection
```python
# ВСЕ запросы используют параметризацию:
cur.execute("SELECT * FROM city WHERE name = %s", (name,))
cur.execute("INSERT INTO route (name, ...) VALUES (%s, %s, ...)", vals)
```

### Валидация данных
- ✅ Проверка типов данных перед вставкой
- ✅ Проверка длины строк (города: 100 символов, маршруты: 255)
- ✅ Проверка диапазонов значений (цена >= 0)
- ✅ Проверка обязательных полей (NOT NULL)
- ✅ Проверка существования связанных записей (Foreign Key)

### Целостность данных
- ✅ Уникальность названий городов
- ✅ Каскадная проверка при удалении
- ✅ Транзакции с rollback при ошибках
- ✅ Обработка всех исключений PostgreSQL

## Установка и настройка

### Системные требования
- Python 3.8 или выше
- PostgreSQL 12 или выше
- 100 МБ свободного места на диске

### Установка зависимостей

#### Через pip:
```bash
pip install psycopg2-binary pyyaml
```

#### Через poetry (рекомендуется):
```bash
poetry install
poetry shell
```

#### requirements.txt:
```txt
psycopg2-binary>=2.9.0
PyYAML>=6.0
```

### Настройка подключения к базе данных

Отредактируйте файл `config.yaml`:

```yaml
dbname: postgres          # Имя базы данных
user: your_username       # Ваш логин PostgreSQL
password: your_password   # Ваш пароль PostgreSQL
host: localhost           # Адрес сервера БД (или IP)
dbtableprefix: "public."  # Префикс схемы
```

**Важно:** Убедитесь, что PostgreSQL запущен и доступен!

### Проверка подключения

```bash
# Проверка PostgreSQL
psql -U your_username -d postgres -h localhost

# Проверка Python модулей
python -c "import psycopg2, yaml; print('OK')"
```

## Запуск приложения

### Первый запуск

```bash
# 1. Перейдите в каталог проекта
cd tour_management_system

# 2. Запустите приложение
python main.py

# 3. В главном меню выберите "2" для инициализации таблиц
# 4. Система создаст таблицы и заполнит тестовыми данными
```

### Обычный запуск

```bash
python main.py
```

## Структура проекта

```
tour_management_system/
├── main.py                 # Главный файл с интерфейсом
├── config.yaml             # Конфигурация подключения к БД
├── project_config.py       # Класс для чтения конфигурации
├── dbconnection.py         # Класс подключения к PostgreSQL
├── dbtable.py              # Базовый класс для работы с таблицами
├── city_table.py           # Класс таблицы городов
├── route_table.py          # Класс таблицы маршрутов
├── README.md               # Документация (этот файл)
└── requirements.txt        # Зависимости проекта
```

## Руководство пользователя

### Главное меню

```
╔════════════════════════════════════════════════════╗
║   СИСТЕМА УПРАВЛЕНИЯ ЭКСКУРСИОННЫМИ ТУРАМИ         ║
╚════════════════════════════════════════════════════╝

Основное меню:
  1 - Просмотр городов
  2 - Сброс и инициализация таблиц
  9 - Выход
```

### Пример работы: Добавление города

1. В главном меню выберите **1** (Просмотр городов)
2. В меню городов выберите **3** (Добавить новый город)
3. Введите название города, например: "Екатеринбург"
4. Система проверит уникальность и добавит город

### Пример работы: Работа с маршрутами

1. В главном меню выберите **1** (Просмотр городов)
2. Выберите **6** (Просмотреть маршруты города)
3. Укажите номер строки города из списка
4. Откроется список маршрутов для выбранного города
5. Доступные операции:
   - **7** - Добавить новый маршрут
   - **8** - Редактировать маршрут
   - **9** - Удалить маршрут

### Постраничная навигация

Когда записей больше 10, появляются кнопки навигации:
- **[** - Предыдущая страница
- **]** - Следующая страница

### Отмена операций

В любом диалоге ввода можно ввести **0** для отмены операции.

## Примеры использования

### Сценарий 1: Добавление города и маршрута

```
1. Главное меню → 1 (Города)
2. Операции → 3 (Добавить город)
3. Ввод: "Владивосток"
4. Операции → 6 (Маршруты города)
5. Выбор города: 5 (Владивосток)
6. Операции → 7 (Добавить маршрут)
7. Название: "Тихоокеанское побережье"
8. Описание: "Путешествие по берегу Тихого океана"
9. Цена: 25000
```

### Сценарий 2: Редактирование маршрута

```
1. Главное меню → 1 (Города)
2. Операции → 6 (Маршруты города)
3. Выбор города: 1 (Москва)
4. Операции → 8 (Редактировать)
5. Номер маршрута: 1
6. Новое название: Enter (без изменений)
7. Новое описание: "Обновленное описание"
8. Новая цена: 16000
```

### Сценарий 3: Удаление города

```
1. Главное меню → 1 (Города)
2. Операции → 5 (Удалить город)
3. Номер города: 4
4. Подтверждение: да

Примечание: Если у города есть маршруты, система откажет 
в удалении и попросит сначала удалить маршруты.
```

## Обработка ошибок

### Сообщения об ошибках

- **✓** - Операция выполнена успешно
- **✗** - Ошибка выполнения операции

### Типичные ошибки

1. **Город с таким названием уже существует**
   - Причина: Попытка добавить дубликат
   - Решение: Используйте уникальное название

2. **Невозможно удалить город: существует N маршрут(ов)**
   - Причина: Есть связанные маршруты
   - Решение: Сначала удалите маршруты

3. **Цена не может быть отрицательной**
   - Причина: Введено отрицательное значение
   - Решение: Введите положительное число

4. **Ошибка подключения к БД**
   - Причина: PostgreSQL не доступен
   - Решение: Проверьте config.yaml и запустите PostgreSQL

## Технические детали

### Стандарты кодирования

- ✅ Соответствие стандарту **ruff**
- ✅ Максимальная длина строки: **86 символов**
- ✅ Type hints для параметров функций
- ✅ Docstrings для всех классов и методов
- ✅ PEP 8 compliant

### Архитектура

```
Main (управление UI)
  ↓
CityTable / RouteTable (бизнес-логика)
  ↓
DbTable (базовые операции)
  ↓
DbConnection (подключение к БД)
  ↓
PostgreSQL
```

### Параметры пагинации

- Размер страницы: **10 записей**
- Настраивается в `Main.PAGE_SIZE`

## Часто задаваемые вопросы

**В: Как сбросить базу данных?**
О: Выберите пункт "2" в главном меню и подтвердите действие.

**В: Можно ли изменить размер страницы?**
О: Да, измените значение `PAGE_SIZE = 10` в классе `Main`.

**В: Как экспортировать данные?**
О: Используйте стандартные средства PostgreSQL (pg_dump).

**В: Поддерживается ли работа с несколькими БД?**
О: Да, создайте несколько config.yaml файлов.

## Решение проблем

### Проблема: "ModuleNotFoundError: No module named 'psycopg2'"

**Решение:**
```bash
pip install psycopg2-binary
```

### Проблема: "could not connect to server"

**Решение:**
1. Проверьте, запущен ли PostgreSQL
2. Проверьте config.yaml (host, port)
3. Проверьте права доступа (user, password)

### Проблема: "permission denied for table"

**Решение:**
```sql
-- В psql выполните:
GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO your_user;
GRANT ALL PRIVILEGES ON ALL SEQUENCES IN SCHEMA public TO your_user;
```

## Разработка и тестирование

### Запуск тестов

```bash
# После добавления тестов
pytest tests/
```

### Проверка кода

```bash
# Проверка стиля
ruff check .

# Форматирование
ruff format .
```

## Лицензия

Учебный проект. Свободное использование в образовательных целях.

## Автор

Разработано для курса по работе с базами данных PostgreSQL.

## История изменений

### Версия 2.0 (Текущая)
- ✅ Переход на таблицы City и Route
- ✅ Полная защита от SQL-injection
- ✅ Постраничная навигация
- ✅ CRUD для обеих таблиц
- ✅ Улучшенная валидация данных
- ✅ Соответствие стандарту ruff

### Версия 1.0
- Базовая версия с таблицами people и phones

## Контакты и поддержка

При возникновении вопросов обращайтесь к преподавателю курса.

---

**Приятной работы с системой управления экскурсионными турами!** 🚀
